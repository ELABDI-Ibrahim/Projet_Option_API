{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y libglib2.0-0 libnss3 libatk1.0-0 libx11-xcb1 libxcomposite1 libxrandr2 libasound2 libcups2 libxdamage1 libgbm1 libpango1.0-0 libgtk-3-0 libxss1 libxtst6 wget ca-certificates && pip install -r requirements.txt && playwright install chromium"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
